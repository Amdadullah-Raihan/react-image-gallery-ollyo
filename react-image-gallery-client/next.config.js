/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['i.ibb.co'], // Add your image hosting domain(s) here
  },
    reactStrictMode: false,
}

module.exports = nextConfig
